name: MergeQueue

on:
  workflow_dispatch:

jobs:
  merge-queue:
    name: Run MergeQueue checks
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: build
        run: sleep 20s 
